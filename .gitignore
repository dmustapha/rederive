.venv/
__pycache__/
*.pyc
.pytest_cache/
*.db
*.db.amnesia
node_modules/
.next/
.env
.env.local

# Baked warm-seed demo fixture (committed on purpose — deploy re-hydrates from it)
!api/seed/memory.warm.db

# secrets / local
.deploy-secrets.env
web/.vercel/

# pipeline state + logs (never published)
.deploy-state.json
*-state.json
docs/pipeline/
.ship-check.json

# demo video project (built locally, delivered via submission platform)
video/
# dev-only console direction explorations (canonical console is /console)
web/app/lab/
